mixin tabs(list=[{id:"A", name:"Tab A"},{id:"B", name:"Tab B"}])
    ul.nav.nav-tabs(role="tablist")
        each val,index in list
            if index == 0
                li.nav-item
                    a.active(data-toggle="tab" href="#tab"+(val.id))= val.name
            else
                li.nav-item
                    a(data-toggle="tab" href="#tab"+(val.id))= val.name
    .tab-content.p-1.p-sm-3
        if block
            block
mixin tab-item(id='A',active='false')
    if active == 'true'
        .container.tab-pane.active.p-0(id="tab"+(id))
            .row.no-gutters
                .col
                    if block
                        block
    else
        .container.tab-pane.p-0(id="tab"+(id))
            .row.no-gutters
                .col
                    if block
                        block